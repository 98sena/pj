<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <!-- CSS only -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
  <script src="../app.js"></script>
  <title>게시판</title>
</head>
<body>
  <div class="container">
  <h1 class="text-center">게시판!!</h1><br>
  <!-- 필터 -->
  <form action="/" method="GET" class="float-end"> <!-- action="/"은 라우터가 localhost이기 때문에 필요. post는 쿼리 url을 얻지 못하기때문에 get사용 -->
    <table>
      <tr >
        <td>
          <input type="text" name="filter" class="form-control" placeholder="검색" value="<%= filter ? filter:'' %>">
        </td>
        <td>
          <button type="submit" class="btn btn-info">검색</button>
        </td>
      </tr>
    </table>
  </form>

  <a href="/add" class="btn btn-primary">등록</a>
  <table class="table table-hover">
    <thead>
      <tr>
        <th scope="col" >number</th>
        <th scope="col">content</th>
        <th scope="col">writer</th>
        <th scope="col">create</th>
        <th scope="col"></th>
      </tr>
    </thead>
    <tbody>
        <% datas.forEach((data, number) => { %>
          <tr>
            <th scope="row" style="width: 8px;"> <%= number += 1 %> </th>
            <td style="width: 500px;"> <%= data.content %> </td>
            <td> <%= data.writer %> </td>
            <td style="width: 8px;"> <%= data.create %> </td>
            <td style="width: 300px;">
              <a href="/edit/<%= data.number %>">
                <button type="button" 
                class="btn btn-secondary float-end"
                style='margin-right: 10px;'>수정하기</button> 
                </a> 
                
              <a href="/delete/<%= data.number %>"> 
              <button type="button" 
                class="btn btn-secondary float-end"
                style='margin-right: 10px;'>삭제하기</button> 
              </a>
            </td>
          </tr>
        <% }) %>
    </tbody> 
  </table>
</div>
</body>
</html>